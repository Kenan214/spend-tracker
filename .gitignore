__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# built app bundle — rebuild locally with `osacompile -o "Spend Tracker.app" launcher.applescript`
Spend Tracker.app/

# packaging/build_release.sh output — built fresh per release, not committed
dist/

# local data — never commit personal financial data
data/*.db
data/raw/*.csv
!data/raw/.gitkeep
